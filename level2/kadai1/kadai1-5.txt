【演習5】
Pythonにおいてすべての値はオブジェクトとしてメモリ上に存在し、変数はそのオブジェクトを指す参照である。

整数型・浮動小数点型・文字列型において：
計算や代入によって値が変わったように見えても、実際には元のオブジェクトは変更されていない。
新しいオブジェクトがメモリ上に作成され、変数の参照先が切り替わる。
このため、これらの型はイミュータブルと呼ばれる。

リストや辞書において：
同一のオブジェクトが指すメモリ領域の中身を直接変更できる設計になっており、
参照先は変わらないまま内容だけが更新されるため、ミュータブルである。
